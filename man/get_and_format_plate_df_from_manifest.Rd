% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_plate_df.R
\name{get_and_format_plate_df_from_manifest}
\alias{get_and_format_plate_df_from_manifest}
\title{Prepare plate data frame from full manifest}
\usage{
get_and_format_plate_df_from_manifest(
  manifest_df,
  plateID,
  columns_for_scoring,
  cols_to_categorize,
  imbalance_fixer = FALSE,
  plate_size,
  plate_wells,
  internal_control_well_indices,
  internal_control_ids
)
}
\arguments{
\item{manifest_df}{Data frame or tibble with a \code{SampleID} column, a plate column (default: "plate", but this can be changed with \code{plate_col} argument), and additional columns for each variable to be used for plate design score. See \link{input_manifest} for an example.}

\item{plateID}{Character vector. Value from column specified by \code{plate_col} to generate a  deconvolved plate for. If NULL (default), every plate in the manifest will be deconvolved.}

\item{columns_for_scoring}{Character vector. Names of columns to use for calculating plate design score.}

\item{imbalance_fixer}{FALSE (default) or length 4 list. First element is logical, second element is character string of a column name, third element is a list of well IDs, and fourth element is a numeric scalar. \strong{TO DO: Avi, explain this.}}

\item{plate_size}{Numeric scalar. Size of plate. Note that currently \code{easyplater} is currently only implemented for 96-well plates.}

\item{plate_wells}{Character vector of length \code{plate_size}. Well names.}

\item{internal_control_well_indices}{(Deprecated) Numeric vector containing indices of control wells. Expecting zero index, and numbering going first top to bottom, then left to right. This argument is ignored if \code{fixed_wells} argument is used.}

\item{internal_control_ids}{(Deprecated) Character vector. Names of internal control wells. This argument is ignored if \code{fixed_wells} argument is used.}
}
\value{
List of length two.
\itemize{
\item The first element is data.frame for the specified plate with an optional additional column \code{imbalanceFix_vec}.
\item The second element is a tibble with columns "SampleID", "plate", "column", "row", "well", plus columns specified in \code{columns_for_scoring} argument.
}
}
\description{
This function subsets a single plate from the input manifest and wrangles the data for downstream processing. \strong{TO DO: Micah, elaborate on this.}
}
\examples{
# We can use easyplater's built-in example manifest
str(input_manifest)
easyplater:::get_and_format_plate_df_from_manifest(
  manifest_df = input_manifest,
  plateID = "plate 1",
  columns_for_scoring = c("Cohort", "Group", "Sex", "AgeGroup"),
  plate_size = 96,
  plate_wells = paste0(rep(LETTERS[1:8], times = 12), rep(1:12, each = 8)),
  internal_control_well_indices = 86:95,
  internal_control_ids = c(paste0("SC", 1:2), paste0("NC", 1:3), paste0("PC", 1:5))
)
}
