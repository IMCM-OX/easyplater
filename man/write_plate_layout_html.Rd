% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_output.R
\name{write_plate_layout_html}
\alias{write_plate_layout_html}
\title{Write plate layouts to html report}
\usage{
write_plate_layout_html(
  manifest_df,
  html_filepath = "plate_layouts.html",
  plate_col = "plate",
  color_by = NULL,
  plate_size = 96,
  include_label = NULL,
  include_legend = NULL,
  html_title = "Plate layouts",
  output_format = "html_document",
  fig_height = 8,
  fig_width = 10,
  rmd_template = NULL
)
}
\arguments{
\item{manifest_df}{Data frame (or tibble). Output of \code{\link[=make_easyplater_design]{make_easyplater_design()}}.}

\item{html_filepath}{String. Path to output file.}

\item{plate_col}{String. Name of column indicating the plate ID.}

\item{color_by}{String. Names of columns containing variables to be displayed in plate wells. Defaults to all columns except "plate", "column", "row", "well", and the one specified by the \code{plate_col} argument.}

\item{plate_size}{Numeric. Note: This function is currently only implemented for 96-well plates.}

\item{include_label}{Character vector. Names of variables to be labeled in plate wells. Defaults to be identical to \code{color_by} argument. It can be handy to leave out columms with variables that are too large to display.}

\item{include_legend}{Character vector. Names of variables for which to plot a legend. Defaults to be identical to \code{color_by} argument, except "SampleID". It can be handy to leave out columms with too many unique values to display in a legend.}

\item{html_title}{String. Main title of html report.}

\item{output_format}{The R Markdown output format to convert to. This should either be "html_document" (default) or an output format object (e.g. \code{\link[rmarkdown:html_document]{rmarkdown::html_document()}} or \code{\link[rmdformats:robobook]{rmdformats::robobook()}})}

\item{fig_height}{Numeric. Figure height in inches.}

\item{fig_width}{Numeric. Figure width in inches.}

\item{rmd_template}{The input R Markdown file to be rendered. See \verb{Customizing the html output} below for details.}
}
\value{
The compiled document is written into the output file, and the path of the output file is returned.
}
\description{
Writes plate layouts from an easyplater output manifest to an html report. Each plate is in a separate section, and each variable is organized into tabs.
}
\details{
\subsection{Customizing the html output}{

The default R Markdown template wrapping \code{\link[OlinkAnalyze:olink_displayPlateLayout]{OlinkAnalyze::olink_displayPlateLayout()}} and specifying how to format the report can be found by running \code{fs::path_package("extdata", "plate_layouts-format.Rmd", package = "easyplater")}. To customize the output html format, you can copy this file and edit it as desired and pass it to \code{write_plate_layout_html()} with the \code{rmd_template} argument.
}
}
\examples{
\dontrun{
write_plate_layout_html(output_manifest)
}
}
